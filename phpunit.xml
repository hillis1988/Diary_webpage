<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnRisky="true"
         failOnWarning="true"
         beStrictAboutOutputDuringTests="true"
         displayDetailsOnTestsThatTriggerWarnings="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Property">
            <directory>tests/Property</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
    </source>

    <php>
        <!-- The property suite generates tens of thousands of cases per run and
             does not fit in PHP's default 128M. -->
        <ini name="memory_limit" value="1G"/>

        <!-- Integration tests connect to a throwaway MariaDB schema; never point these at production. -->
        <env name="DIARY_TEST_DB_HOST" value="127.0.0.1"/>
        <env name="DIARY_TEST_DB_PORT" value="3306"/>
        <env name="DIARY_TEST_DB_NAME" value="diary_test"/>
        <env name="DIARY_TEST_DB_USER" value="root"/>
        <env name="DIARY_TEST_DB_PASSWORD" value="root"/>
    </php>
</phpunit>
